<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.dong.member.MemberDAO">

	<!-- 로그인 -->
	<select id="login" parameterType="kr.co.dong.member.MemberDTO"
		resultType="kr.co.dong.member.MemberDTO">
		SELECT id, password, name, phone, email, birth_date, gender, notification, region, role,
		is_active, created_at
		FROM users
		WHERE id = #{id} AND password = #{password}
	</select>

	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="kr.co.dong.member.MemberDTO">
    INSERT INTO users
    (id, password, name, phone, email, birth_date, gender, notification, region)
    VALUES
    (#{id}, #{password}, #{name}, #{phone}, #{email}, #{birth_date}, #{gender}, #{notification}, #{region})
</insert>

	<!-- 아이디 중복 체크 -->
	<select id="idCheck" parameterType="String" resultType="int">
		SELECT
		COUNT(*) FROM users WHERE id = #{id}
	</select>
	
	<!-- 회원 업데이트 -->
	<update id="userupdate" parameterType="kr.co.dong.member.MemberDTO">
		update users set name = #{name}, email = #{email}, birth_date = #{birth_date}, gender = #{gender}, region = #{region} 
		where id = #{id}
	</update>
	
	<!-- 관리자 회원 업데이트 -->
	<update id="adminupdate" parameterType="kr.co.dong.member.MemberDTO">
		update users set name = #{name}, email = #{email}, birth_date = #{birth_date}, gender = #{gender}, region = #{region}, role = #{role} 
		where id = #{id}
		
	</update>
	<!-- 회원 비밀번호 업데이트 -->
	<update id="updatePassword" parameterType="kr.co.dong.member.MemberDTO">
    UPDATE users SET password = #{password}
    WHERE id = #{id}
	</update>

	<!-- 내정보 불러오기 -->
	<select id="myDTO" parameterType="String"  resultType="kr.co.dong.member.MemberDTO">
		select id, password, name, phone, email, birth_date, gender, notification, region, role,
		is_active, created_at
		FROM users
		WHERE id = #{id}
	</select>

	<select id="memberCount" resultType="int">
		select count(*) from users
	</select>
	
	<!-- 모든 회원 리스트 -->

	<select id="allDTO" resultType="kr.co.dong.member.MemberDTO">
		select id, password, name, phone, email, birth_date, gender, notification, region, role,
		is_active, created_at
		FROM users 
	</select>
	
	<!-- 전체/조건 검색 -->
	<select id="searchMembers" parameterType="map" resultType="kr.co.dong.member.MemberDTO">
	    SELECT *
	    FROM users
	    <where>
	        <choose>
	            <!-- 전체 검색 -->
	            <when test="searchType == 'all' and searchValue != null and searchValue != ''">
	                id LIKE CONCAT('%', #{searchValue}, '%')
	                OR name LIKE CONCAT('%', #{searchValue}, '%')
	                OR phone LIKE CONCAT('%', #{searchValue}, '%')
	                OR email LIKE CONCAT('%', #{searchValue}, '%')
	                OR DATE_FORMAT(birth_date, '%Y-%m-%d') LIKE CONCAT('%', #{searchValue}, '%')
	                OR gender LIKE CONCAT('%', #{searchValue}, '%')
	                OR notification LIKE CONCAT('%', #{searchValue}, '%')
	                OR region LIKE CONCAT('%', #{searchValue}, '%')
	                OR role LIKE CONCAT('%', #{searchValue}, '%')
	                OR is_active LIKE CONCAT('%', #{searchValue}, '%')
	                OR DATE_FORMAT(created_at, '%Y-%m-%d') LIKE CONCAT('%', #{searchValue}, '%')
	            </when>
	
	            <!-- 특정 컬럼 검색 -->
	            <when test="searchType != 'all' and searchValue != null and searchValue != ''">
	                <choose>
	                    <!-- 숫자 타입 컬럼 -->
	                    <when test="searchType == 'notification' or searchType == 'is_active'">
	                        CAST(${searchType} AS CHAR) LIKE CONCAT('%', #{searchValue}, '%')
	                    </when>
	
	                    <!-- 날짜 타입 컬럼 -->
	                    <when test="searchType == 'birth_date' or searchType == 'created_at'">
	                        DATE_FORMAT(${searchType}, '%Y-%m-%d') LIKE CONCAT('%', #{searchValue}, '%')
	                    </when>
	
	                    <!-- 나머지 문자열 컬럼 -->
	                    <otherwise>
	                        ${searchType} LIKE CONCAT('%', #{searchValue}, '%')
	                    </otherwise>
	                </choose>
	            </when>
	        </choose>
	    </where>
	</select>
	
	<select id="selectone" parameterType="String" resultType="kr.co.dong.member.MemberDTO">
		select id, password, name, phone, email, birth_date, gender, notification, region, role,
		is_active, created_at
		FROM users
	    WHERE id = #{id}
	</select>
	
	<!-- 회원 탈퇴  -->
	<update id="deleteUser" parameterType="String">
	    UPDATE users SET is_active = 0 WHERE id = #{id}
	</update>
</mapper>